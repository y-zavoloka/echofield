version: "3.9"

services:
  web:
    # Use the image built and pushed to GHCR by GitHub Actions
    # The tag is injected via IMAGE_TAG in the .env file generated by the deploy workflow.
    image: ghcr.io/echo/echofield:${IMAGE_TAG}
    # Disable any build context in this override so prod never rebuilds locally
    build: null
